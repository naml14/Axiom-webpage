{
  "name": "axiom-webpage",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/server.ts",
    "build": "bun run src/build.ts",
    "start": "bun run src/server.ts",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "axiom-framework": "^0.9.1"
  },
  "devDependencies": {
    "@types/bun": "1.3.11",
    "typescript": "^6.0.2"
  }
}
